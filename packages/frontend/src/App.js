import React from "react";
import { View } from "react-native";

import AppNavigator from "./navigator";

export default () => {
	return (
		<React.Fragment>
			<AppNavigator />
		</React.Fragment>
	);
};

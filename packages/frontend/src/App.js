import React from "react";
import { View } from "react-native";

import AppNavigator from "navigator";

export default () => {
	return (
		<React.Fragment>
			<View>hi</View>
			<AppNavigator />
		</React.Fragment>
	);
};
